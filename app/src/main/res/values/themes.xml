<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.App.Splash" parent="Theme.SplashScreen">
        <!-- Ícono grande del splash -->
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <!-- Color de fondo del splash -->
        <item name="windowSplashScreenBackground">@color/purple_700</item>
        <!-- (Opcional) Duración si agregas icono animado de compat -->
        <item name="postSplashScreenTheme">@style/Theme.AplicacionesIOTTheme</item>
    </style>

    <style name="Theme.AplicacionesIOTTheme" parent="android:Theme.Material.Light.NoActionBar" />
</resources>